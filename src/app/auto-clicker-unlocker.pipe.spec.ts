import { AutoClickerUnlockerPipe } from './auto-clicker-unlocker.pipe';

describe('AutoClickerUnlockerPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoClickerUnlockerPipe();
    expect(pipe).toBeTruthy();
  });
});
